pipeline {
    agent any
    stages {
        stage('pre-build') {
        //     steps {
        //         sh 'echo "$DOCKER_PASSWORD" | docker login "$DOCKER_REGISTRY" --username "$DOCKER_USERNAME" --password-stdin'
        //     }
        // }
        stage('build') {
            steps {
                echo "Hello"
            }
        }
    }
}
